# Excel Source

The analysis originated from the supplied e-commerce order-line workbook.

The repository keeps the existing root CSV for compatibility with the current Power BI workflow. Before moving or renaming the source locally, confirm and update the PBIX source path so the report remains reproducible.

See `data/DATA_DICTIONARY.md` for field definitions and `analysis/ANALYSIS_NOTES.md` for validated metrics.
